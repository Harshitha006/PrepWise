// ============================================================
// app/(root)/resume/page.tsx  (NEW — Resume Analyzer page)
// ============================================================

import ResumeAnalyzer from "@/components/ResumeAnalyzer";

export default function ResumePage() {
  return <ResumeAnalyzer />;
}